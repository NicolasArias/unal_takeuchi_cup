# Environment

- We use WSL, so we should always use Linux commands.
- Because we are using Windows through WSL, the root of the project is `/mnt/c/Users/klaus/OneDrive/Desktop/code/unal_takeuchi_cup`.
