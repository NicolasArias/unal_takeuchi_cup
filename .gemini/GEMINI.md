# Project System Preferences
- Environment: **WSL (Linux)**
- Commands: ALWAYS use Linux/Bash syntax.
- Style: Prefer `ls` over `dir`, `cat` over `type`, and ensure path separators are `/`.
- Language: All code, comments, and documentation must be in English, except for user-facing UI in the `front` directory.
